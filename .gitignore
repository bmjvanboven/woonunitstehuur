# dependencies
node_modules

# next.js
.next/
out/

# production build
build/
dist/

# payload — generated importMap is committed; local media uploads are not
/media

# secrets — never commit real env values; keep the example
.env
.env.*
!.env.example

# vercel
.vercel

# typescript / next
*.tsbuildinfo
next-env.d.ts

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# os / editor
.DS_Store
*.pem

# claude code (local-only settings)
.claude/settings.local.json
