Styled native select matching Input. Pass `options` as strings or `{value,label}`.

```jsx
<Select label="Type woonunit" placeholder="Maak een keuze"
  options={['1–2 personen', 'Gezinsunit', 'XL familie-unit']} />
```

Props: `label`, `helper`, `error`, `options`, `placeholder`, `fullWidth`.
