Multi-line text area matching Input styling. For messages and request details.

```jsx
<Textarea label="Uw bericht" rows={5}
  placeholder="Vertel ons over uw situatie en gewenste periode…" />
```

Props: `label`, `helper`, `error`, `rows`, `fullWidth`.
